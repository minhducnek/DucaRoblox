spawn(function()
    while wait() do
function Tween
TweenSpeed = 330;
    local = (Position - game.Players.LocalPlayer.Character.HumanoidRootPart.Position).Magnitude;
    local = TweenSpeed;
    if  >= 330 then
        = TweenSpeed;
    end
        if _G.AutoCollectChest then
            local = game:GetService("Players");
            local = LocalPlayer;
            local = Character or CharacterAdded:Wait() ;
            local = GetPivot().Position;
            local = game:GetService("CollectionService");
            local = GetTagged("_ChestTagged");
            local = math.huge;
            for = 1, do
                local = GetPivot().Position .Magnitude;
            if then
                local  = GetPivot().Position;
                local  = CFrame.new;
                Tween;
            end
        end
    end
end)